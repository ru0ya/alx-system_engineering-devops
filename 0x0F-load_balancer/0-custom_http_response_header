#!/bin/env bash
#configures nginx so that http request can have custom header
sudo apt-get update
sudo apt-get install nginx -y
echo "add_header X-Served-By \$hostname;" | sudo tee -a /etc/nginx/nginx.conf
sudo service nginx restart
